package com.example.financemanagement.ui.theme

/**
 * Placeholder theme utilities.
 *
 * This project currently uses ViewBinding / traditional Views. The previous file
 * contained Jetpack Compose @Composable usages which require Compose compiler
 * and runtime in the classpath. To avoid adding Compose as a dependency right
 * now, this file is a small placeholder that teams can replace with their own
 * theme helpers (Compose or non-Compose) as needed.
 */
object Theme {
    const val APP_NAME = "FinanceManagement"
}
